{
  "name": "Don't force the desktop mode on synced YouTube tabs",
  "description": "This small browser extension stops YouTube from forcing desktop mode on synced tabs by removing the app=desktop query parameter.",
  "version": "1.0",
  "manifest_version": 3,
  "permissions": ["webNavigation"],
  "host_permissions": [
    "https://*.youtube.com/*"
  ],
  "background": {
    "service_worker": "worker.js"
  }
}